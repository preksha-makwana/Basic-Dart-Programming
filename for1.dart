// Write a program to print odd numbers till 5000
import 'dart:io';

void main() {
  int number = 1;

  // for( initializing  ; condition ; increment/decerement  )

  for (number = 1; number < 5000; number += 2) {
    print(number);
  }
  // number = number + 2;
  // print(number);
  // number = number + 2;
  // print(number);
  // number = number + 2;
  // print(number);
  // number = number + 2;
  // print(number);
  // number = number + 2;
  // print(number);
}
