// Write a program to print "Hello world " 100 times
import 'dart:io';

void main() {
  String name = 'Hello World ';
  int count = 0;

  while (count < 100) {
    stdout.write(name);
    count = count + 1;
  }

  // print(name);
  // count = count + 1;

  // print(name);
  // count = count + 1;

  // print(name);
  // count = count + 1;
}
